"use client";

import { ButtonExamples } from "@/components/common/Button.example";

/**
 * Button 컴포넌트 테스트 페이지
 *
 * 개발 중에 /button-test 경로로 접속하여 버튼 컴포넌트를 확인할 수 있습니다.
 * 프로덕션 배포 시에는 이 페이지를 삭제하거나 접근을 제한하세요.
 */
export default function ButtonTestPage() {
  return <ButtonExamples />;
}
